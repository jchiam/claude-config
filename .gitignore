# Credentials and secrets
.credentials.json

# Settings (generated by setup-settings.sh — contains credentials)
settings.json

# Daemon and job runtime
daemon/
daemon.lock
daemon.log
daemon.status.json
daemon-auth-cooldown
daemon-auth-status.json
jobs/
statusline-command.sh

# Runtime and ephemeral state
backups/
cache/
downloads/
file-history/
history.jsonl
ide/
paste-cache/
plans/
projects/
memory/
session-env/
sessions/
shell-snapshots/
tasks/
telemetry/

# Feature flags / temp files
.claude-auth-profile
.claude-auth-token
.caveman-active
.gitkeep

# Runtime timestamps and update state
.last-cleanup
.last-update-result.json
plugins/.last_inuse_sweep

# Local settings overrides (may contain credentials)
settings.local.json

# Plugin runtime (manifest files are committed; cache/data/marketplaces are not)
plugins/cache/
plugins/data/
plugins/marketplaces/
plugins/install-counts-cache.json
plugins/plugin-catalog-cache.json
plugins/blocklist.json
plugins/known_marketplaces.json

# MCP runtime state
mcp-needs-auth-cache.json

# Serverless telemetry
sls-telemetry/
